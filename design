Use the FHIR questionnaire JSON structure at top-level.

Bind 

 * sub-groups to <group> directives
 * questions to <question> directives

Each directive defines a template creating rendering + controllers as needed.
